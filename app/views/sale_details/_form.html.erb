<%= form_for([@sale, @sale_detail], :html => { :class => "form-horizontal" }, :remote => true) do |f| %>
  <% if @sale_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sale_detail.errors.count, "error") %> prohibited this sale_detail from being saved:</h2>

      <ul>
      <% @sale_detail.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :product, "#{t 'sale.detail.product'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.select(:product_id, @products.collect {|product| ["#{product.name} #{product.product_type}-#{product.merk} (#{product.barcode_id})", product.id,]}, {:prompt => "#{t 'prompt'} #{t 'sale.detail.product'}"}, :class => "form-control") %></div>
  </div>
  <div class="form-group">
    <%= f.label :quantity, "#{t 'sale.detail.quantity'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :quantity, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :price, "#{t 'sale.detail.price'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :price, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :discount, "#{t 'sale.detail.discount'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :discount, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :added_discount, "#{t 'sale.detail.added_discount'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :added_discount, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-sm-4"></div>
    <div class="col-sm-6"><%= f.submit "#{t 'submit'}", :class => "btn btn-success btn-block", :disable_with => "#{t 'disable_with'}" %></div>
  </div>
<% end %>
