$("#modal").dialog({
  autoOpen: true,
  height: 300,
  width: 450,
  modal: true,
  title: "<%= t 'country.country' %> - <%= t 'edit' %>",
  open: function() {
    $(this).html("<%= escape_javascript(render('form')) %>")
  },
  buttons: {
    "<%= t 'cancel' %>": function() { $(this).dialog("close"); }
  }
});