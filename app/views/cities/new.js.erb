$("#modal").dialog({
  autoOpen: true,
  height: 380,
  width: 450,
  modal: true,
  title: "<%= t 'city.city' %> - <%= t 'new' %>",
  open: function() {
    $(this).html("<%= escape_javascript(render('form')) %>")
  },
  buttons: {
    "<%= t 'cancel' %>": function() { $(this).dialog("close"); }
  }
});