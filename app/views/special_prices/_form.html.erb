<%= form_for(@special_price, :html => { :class => "form-horizontal"}, :remote => true) do |f| %>
  <% if @special_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@special_price.errors.count, "error") %> prohibited this special_price from being saved:</h2>

      <ul>
      <% @special_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :description, "#{t 'special_price.description'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :description, :required => true, :autofocus => true, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :price_each_size, "#{t 'special_price.price_each_size'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :price_each_size, :required => true, :autofocus => true, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-sm-4"></div>
    <div class="col-sm-6"><%= f.submit "#{t 'submit'}", :class => "btn btn-success", :disable_with => "#{t 'disable_with'}" %></div>
  </div>
<% end %>
