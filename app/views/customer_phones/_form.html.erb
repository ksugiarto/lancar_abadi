<%= form_for([@customer, @customer_phone], :html => { :class => "form-horizontal" }, :remote => true) do |f| %>
  <% if @customer_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_phone.errors.count, "error") %> prohibited this customer_phone from being saved:</h2>

      <ul>
      <% @customer_phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :country_ext, "#{t 'customer.phone.country_ext'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :country_ext, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :phone_number, "#{t 'customer.phone.phone_number'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :phone_number, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :description, "#{t 'customer.phone.description'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :description, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-sm-4"></div>
    <div class="col-sm-6"><%= f.submit "#{t 'submit'}", :class => "btn btn-success", :disable_with => "#{t 'disable_with'}" %></div>
  </div>
<% end %>
