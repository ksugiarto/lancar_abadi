<%= form_for([@supplier, @supplier_phone], :html => { :class => "form-horizontal" }, :remote => true) do |f| %>
  <% if @supplier_phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplier_phone.errors.count, "error") %> prohibited this supplier_phone from being saved:</h2>

      <ul>
      <% @supplier_phone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :country_ext, "#{t 'supplier.phone.country_ext'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :country_ext, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :phone_number, "#{t 'supplier.phone.phone_number'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :phone_number, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <%= f.label :description, "#{t 'supplier.phone.description'}", :class => "col-sm-4 control-label" %>
    <div class="col-sm-6"><%= f.text_field :description, :class => "form-control" %></div>
  </div>
  <div class="form-group">
    <div class="col-sm-4"></div>
    <div class="col-sm-6"><%= f.submit "#{t 'submit'}", :class => "btn btn-success", :disable_with => "#{t 'disable_with'}" %></div>
  </div>
<% end %>
